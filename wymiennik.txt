def f1(uniq):
	...
def klasa_liczn(el,klucze):
	new_wart = [i[1] for i in klucze]
	new_wart = f1(new_wart)

	liczn = 0
	for i in klucze:
		if(el == i[0]):
			liczn = i[1]
	for i in range(len(new_wart)):
		if(liczn==new_wart[i]):
			return i
